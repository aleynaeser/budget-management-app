import { BMTitle } from '@common/components/Layout';

export default async function NotFound() {
  return (
    <section id='not-found'>
      <BMTitle title='Page Not Found' />
    </section>
  );
}
