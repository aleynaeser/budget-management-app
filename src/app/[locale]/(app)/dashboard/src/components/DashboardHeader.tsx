import { BMTitle } from '@components/Layout';
import React from 'react';

export default function DashboardHeader() {
  return (
    <header id='dashboard-header'>
      <BMTitle title='Your Analytics' />
    </header>
  );
}
